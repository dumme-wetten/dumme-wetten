= link_to 'Neu', new_user_path

%table
  %tr
    %th
      ID
    %th
      Email
    %th
      Name
    %th
      Gelöscht
    %th
      Aktion
  - @users.each do |user|
    %tr
      %td
        = user.id
      %td
        = user.email
      %td
        = user.name
      %td
        = user.deleted
      %td
        = link_to 'Edit', edit_user_path(user)
        = link_to 'Destroy', user_path(user),  method: :delete, data: { confirm: 'Are you sure?' }
